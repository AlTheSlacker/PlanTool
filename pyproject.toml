[project]
name = "plantool"
version = "0.1.0"
description = "LLM-led project-planning tool: interview engine, MCP server, plan database (stage 1)"
requires-python = ">=3.12"
dependencies = ["mcp", "pyyaml"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
